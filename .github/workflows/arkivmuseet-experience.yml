name: Arkivmuseet experience
on:
  pull_request:
    paths:
      - 'arkivmuseet/**'
      - '.github/workflows/arkivmuseet-experience.yml'
permissions:
  contents: read
concurrency:
  group: museum-experience-${{ github.event.pull_request.number }}
  cancel-in-progress: true
jobs:
  experience:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - name: Install, test and build
        working-directory: arkivmuseet
        run: |
          pnpm install --frozen-lockfile
          pnpm test
          pnpm build
      - name: Browser play-through
        working-directory: arkivmuseet
        run: |
          pnpm exec playwright install --with-deps chromium
          pnpm test:browser
      - name: Package review build
        if: always()
        run: tar --exclude=arkivmuseet/node_modules --exclude=arkivmuseet/experience-artifacts -czf museum-workbench.tar.gz arkivmuseet
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: museum-review
          path: |
            museum-workbench.tar.gz
            arkivmuseet/experience-artifacts/
          retention-days: 7
