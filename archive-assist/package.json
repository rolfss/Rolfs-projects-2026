{
  "name": "archive-assist",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.mjs && node --check ai.mjs && node --check extract.mjs && node --check engine.mjs && node --check zip.mjs && node --check control-report.mjs"
  }
}
